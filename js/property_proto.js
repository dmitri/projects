/* simple functions for the prototype */

$( document ).ready(function() {
  // load the includes
  $( "#carousel" ).load( "carousel.html" );
  // pause the carousel
  //$('.carousel').pause();
  
  
});